Run `make` to convert the the events files (in inputs/) into heamodynamic responses and then design matrices (dmtx_*.csv).

Requirements:
- python with nistats and pandas modules
- make
- R and rmarkdown to produce correlation report 

